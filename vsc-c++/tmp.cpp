#include <bits/stdc++.h>
#include <set>
using namespace std;
#define ll long long
typedef pair<int, int> P;
const int inf = 0x3f3f3f3f << 1;
multiset<int> ms;

signed main()
{
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);

    vector<int> mp({1, 2, 3, 4});
    cout << max(mp);

    return 0;
}