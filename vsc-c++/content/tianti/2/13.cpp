#include <bits/stdc++.h>
using namespace std;
#define int long long
typedef pair<int, int> P;

vector<P> v;
bool flag;

signed main()
{
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);

    int _;
    cin >> _;
    while (_--)
    {
        int t, l, r;
        cin >> t >> l >> r;
        
    }

    return 0;
}